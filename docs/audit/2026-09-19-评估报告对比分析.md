# 评估报告对比分析（2026-09-19）

> **触发原因**：用户质疑本次会话评估结论（B- 77.65/100）与《sz-rust 项目成熟度评估报告（2026-09-04）》（B+）差距过大，要求核实哪个准确、是否基于实际代码。

---

## 一、核心发现：两份报告基于的代码范围不同

### 1.1 评估报告（2026-09-04）基于的代码范围

| 项 | 值 | 验证结果 |
|---|---|---------|
| 审查时点 commit | `d81964d` | **❌ 不存在于任何 git ref**（`git log --all --oneline \| grep d81964d` 无结果） |
| workspace members | 37 | 分离前完整仓库（含企业版） |
| .rs 文件 | 667 | 分离前 |
| 代码行数 | 252,826 | 分离前 |
| 测试函数 | 7,894 | 分离前 |
| sz300 | 包含 | 评估报告引用 sz300 5 次作为"生产已接入"证据 |

### 1.2 本次会话评估基于的代码范围

| 项 | 值 | 验证结果 |
|---|---|---------|
| 审查时点 commit | `07a080d`（HEAD） | **✅ 存在** |
| workspace members | 32 | **✅ 实际核实**（`grep -c '^\s*"' Cargo.toml` = 32） |
| .rs 文件 | 592 | **✅ 实际核实**（`find packages -name "*.rs" \| wc -l` = 592） |
| 代码行数 | 230,580 | **✅ 实际核实**（`find ... -exec cat {} + \| wc -l` = 230,580） |
| 测试函数 | 7,122 | **✅ 实际核实**（`grep -rn "#\[test\]\|#\[tokio::test\]" \| wc -l` = 7,122） |
| sz300 | **不包含** | `ls packages/ \| grep sz300` 无结果 |

### 1.3 根本原因

**commit `1614e84`（开源/企业版物理分离，37→29 crate）将 sz300 移到了企业版仓库。**

- 评估报告（2026-09-04）写于分离前后，基于 `d81964d`（分离前的完整仓库，含 sz300）
- 分离后 `d81964d` 被 rebase 覆盖，从 git 历史中消失
- 当前开源仓库（32 crate）不含 sz300，但 README 仍引用 sz300 17 次作为"生产已接入"证据

---

## 二、评估报告引用的 RESOLVED commit 验证

| 缺口 | commit | 存在性 | 说明 |
|------|--------|--------|------|
| P0-2 SDD Agent | `cc8b21a` | **❌ MISSING** | 企业版仓库 commit，不在开源仓库 |
| P0-3 开源/企业版分离 | `1614e84` | ✅ EXISTS | 分离 commit 本身存在 |
| P0-4 插件数据互通 | `d403420` | **❌ MISSING** | 企业版仓库 commit |
| P1-1 AI 迁移工具 | `5ef4fb1` | **❌ MISSING** | 企业版仓库 commit |

**结论**：评估报告标记为 RESOLVED 的 P0-2/P0-4/P1-1 均在企业版仓库实现，当前开源仓库无法验证这些功能是否存在。

---

## 三、两份报告结论对比

| 维度 | 评估报告（2026-09-04） | 本次会话评估 | 差异原因 |
|------|----------------------|------------|---------|
| 工程成熟度 | **B+** | **B- 77.65** | 评估报告含企业版（sz300 + SDD + 插件互通）；本次仅开源版 |
| crate 数量 | 37 | 31（❌ 实际 32） | 本次少数 1 个，已修正 |
| 代码行数 | 252,826 | 19.4 万（❌ 实际 230,580） | 本次少报约 15%，已修正 |
| 测试数量 | 7,894 | 未统计 | 本次未做精确统计 |
| 覆盖率 | 84.91% | 70.8% | 评估报告含 sz300 集成测试；本次仅开源 crate |
| P0-2 SDD Agent | ✅ RESOLVED | 未评估 | 企业版功能，不在开源仓库 |
| P0-4 插件互通 | ✅ RESOLVED | 未评估 | 企业版功能，不在开源仓库 |
| tenant_middleware | 未提及 | ⚠️ 未接线 | 两者都正确：功能存在但无开源版调用方 |
| data_scope_middleware | 未提及 | ⚠️ 仅测试用 | 同上 |
| 孤岛 crate | P2-4 工作流已存在 | ⚠️ 无消费者 | 评估报告看"存在"，本次看"有消费者" |

---

## 四、哪个评估准确？

**两者都"正确"，但评估范围和维度不同：**

### 评估报告（2026-09-04）— 能力完整性维度
- **范围**：完整仓库（开源 29 + 企业版 9 = 38 crate，含 sz300）
- **结论**：B+ — 所有能力存在且测试通过
- **问题**：基于的 commit `d81964d` 已消失，无法复现验证

### 本次会话评估 — 开源版生产接线维度
- **范围**：当前开源仓库（32 crate，不含 sz300）
- **结论**：B- 77.65 — 开源版有未接线能力和孤岛 crate
- **问题**：数字有误差（31→32 crate，19.4 万→23 万行）

### 核心差异
1. **sz300 缺失**：评估报告的所有"生产已接入"证据依赖 sz300，但 sz300 不在当前开源仓库
2. **维度不同**：评估报告看"能力是否存在"，本次看"是否接线到生产"
3. **commit 消失**：评估报告基于的 `d81964d` 已被 rebase 覆盖

---

## 五、本次会话评估的数字修正

| 项 | 原报告值 | 实际值 | 误差 |
|---|---------|--------|------|
| crate 数量 | 31 | **32** | -1（少数 1 个） |
| 代码行数 | 19.4 万 | **230,580**（约 23 万） | -15% |
| 测试函数 | 未精确统计 | **7,122** | — |
| sz-rust-visual 编译 | "编译失败" | **`cargo check` 通过** | 误报 |

---

## 六、建议

1. **评估报告（2026-09-04）需标注**：基于的 commit `d81964d` 已消失，结论适用于"开源+企业版"完整仓库，不适用于当前仅开源仓库
2. **README 需修正**：17 处 sz300 引用应标注"企业版功能"，避免开源用户误解为开源版已具备
3. **后续评估应明确范围**：是评估开源版还是开源+企业版，两者结论不同
4. **本次会话评估数字需修正**：32 crate、23 万行、7122 测试、sz-rust-visual 编译通过

---

*生成：2026-09-19，基于当前 HEAD `07a080d` 实际核实*