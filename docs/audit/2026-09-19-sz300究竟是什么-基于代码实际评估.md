# sz300 究竟是什么 — 基于代码实际的评估（2026-09-19）

> **触发原因**：用户要求深读所有文档和记忆，基于代码实际搞清楚 sz300 究竟是什么。
> **方法**：搜索代码库 + 读取实际源码 + 读取硬件文档 + 读取记忆文档，禁止凭文档自述回答。

---

## 一、结论

**sz300 = 鲜视达 SZ-300 智能秤后端服务** — 一个菜市场智能秤的 SaaS 后端系统，基于 sz-rust 框架开发。

- **硬件产品**：SZ-300 智能秤（菜市场用，语音播报 + AI 商品识别 + 摄像头）
- **软件后端**：sz-rust-sz300（设备/商户/商品/订单管理 + MQTT 硬件通信 + OTA 固件升级）
- **关系**：sz-rust 是框架，sz300 是基于框架的业务应用

---

## 二、代码实证

### 2.1 sz300 源码位置

| 位置 | 路径 | 状态 |
|------|------|------|
| sz-rust-oss 仓库 | `E:/vue/test/鲜视达/rust/sz-rust-oss/packages/sz-rust-sz300` | ✅ 存在，7,068 行 |
| 服务器企业版 | `E:/www/rust/sz-rust-enterprise/packages/sz-rust-sz300` | ✅ 存在 |
| 当前 sz-rust 仓库 | `packages/sz-rust-sz300` | ❌ 不存在（分离时移出） |

### 2.2 入口代码（main.rs:3）

```rust
//! 鲜视达 SZ-300 后端服务入口
```

### 2.3 Cargo.toml 描述（:8）

```toml
description = "SZ-Rust sz300 业务应用 — 设备/商户/商品/订单管理示例"
```

### 2.4 代码规模

```
7,068 行 Rust 代码（src/ 目录）
```

### 2.5 MVC 结构（实际目录）

**Controllers**（10 个）：
- auth（认证）、merchant（商户）、product（商品）、device（设备）、order（订单）
- file（文件上传）、file_serve（静态文件）、health（健康检查）、admin（管理）、common（公共）

**Models**（13 个）：
- merchant、merchant_user、product、category、ai_category（AI 商品分类）
- device、order、order_item、settlement（结算）、market（菜市场）
- ota_version（OTA 固件升级）、operate_log（操作日志）、system_config

**Services**（9 个）：
- auth_service、device_service、merchant_service、product_service、order_service
- file_service、health_service
- mqtt_service + mqtt_listener（MQTT 与智能秤硬件通信）

### 2.6 路由（router.rs 实际注册）

```
# 认证
POST /api/v1/auth/login          # 登录
POST /api/v1/auth/refresh        # 刷新 token
POST /api/v1/auth/me             # 当前用户
POST /api/v1/auth/logout         # 登出

# 商户管理
POST /api/v1/merchant/list       # 商户列表
POST /api/v1/merchant/info       # 商户详情
POST /api/v1/merchant/create     # 创建商户
POST /api/v1/merchant/update     # 更新商户
POST /api/v1/merchant/delete     # 删除商户

# 商品管理
POST /api/v1/product/list        # 商品列表
POST /api/v1/product/info        # 商品详情
POST /api/v1/product/create      # 创建商品
POST /api/v1/product/update      # 更新商品
POST /api/v1/product/delete      # 删除商品

# 设备管理（智能秤）
POST /api/v1/device/list         # 设备列表
POST /api/v1/device/info         # 设备详情
POST /api/v1/device/bind         # 绑定设备
POST /api/v1/device/unbind       # 解绑设备
POST /api/v1/device/ota          # OTA 固件升级
POST /api/v1/device/status_report # 设备状态上报

# 订单管理
POST /api/v1/order/list          # 订单列表
POST /api/v1/order/info          # 订单详情
POST /api/v1/order/create        # 创建订单

# 文件上传
POST /api/v1/file/upload         # 文件上传
POST /api/v1/file/upload_multipart # 多文件上传

# 静态文件
GET  /uploads/{*path}            # 静态文件服务

# 健康检查 + 可观测性
GET  /health                     # 健康检查
GET  /health/ready               # 就绪检查
GET  /health/startup             # 启动检查
GET  /metrics                    # Prometheus 指标

# API 文档
GET  /api-docs                   # Swagger UI
GET  /api-docs/redoc             # Redoc
GET  /api-docs/openapi.json      # OpenAPI JSON
```

### 2.7 中间件链（router.rs:94-100）

```
CORS（最外层）→ CSRF → JWT 鉴权 → 业务 handler
```

---

## 三、硬件产品实证

### 3.1 硬件文档目录

`E:/vue/test/鲜视达/rust/SZ-300智能秤/` 包含：

- `SZ300_Smart_Scale.kicad_sch` — KiCad 原理图
- `SZ-300-AI商品识别模型选型与数据集构建.md`
- `SZ-300-PCB布局指导.md`
- `SZ-300元器件采购与使用指南.md`
- `SZ-Protocol_菜市场场景协议设计_v3.md`
- `产品关键问题深度分析.md` — 菜市场噪音问题 + 语音播报方案
- `智能秤一体机深度优化方案.md`
- `智能采集盒硬件方案设计.md`
- `arduino_tests/` — Arduino 测试代码
- `kicad/` — PCB 设计文件

### 3.2 产品场景（产品关键问题深度分析.md:7-22）

```
早市高峰期（7:00-9:00），一个 200 摊位的菜市场：
"微信收款 5 元"    ← 摊位 A
"支付宝 12 元"     ← 摊位 B
→ 整个市场 70-80 分贝持续噪音
→ 智能秤需要自适应音量调控
```

---

## 四、sz300 与 sz-rust 的关系

### 4.1 框架 vs 应用

- **sz-rust**：Web 框架（对标 ThinkPHP 8），提供路由/中间件/ORM/DI/模板等基础能力
- **sz300**：基于 sz-rust 框架开发的业务应用，实现菜市场智能秤的后端业务逻辑

### 4.2 依赖关系（sz300 Cargo.toml）

```toml
sz-rust-core.workspace = true          # 框架核心
sz-rust-mvc-facade.workspace = true    # MVC facade
sz-rust-middleware-facade.workspace = true  # 中间件
sz-rust-http-facade.workspace = true   # HTTP facade
sz-rust-state-facade.workspace = true  # 状态 facade
sz-rust-observability.workspace = true # 可观测性
sz-orm-sqlx.workspace = true           # ORM SQLx 后端
```

### 4.3 开源/企业版分离

- commit `1614e84`（开源/企业版物理分离）将 sz300 移出当前 sz-rust workspace
- 当前 sz-rust 仓库（32 crate）不含 sz300
- README 17 处引用 sz300 作为"生产已接入"证据，但代码不在当前仓库
- sz300 实际代码在 `sz-rust-oss` 和服务器 `sz-rust-enterprise` 中

---

## 五、记忆文档佐证

| 记忆 | 内容 |
|------|------|
| `project-sz300-not-in-workspace.md` | sz300 不在当前 workspace，是两份报告数字差异的根本原因 |
| `reference-enterprise-repo.md` | 企业版仓库包含 sz-rust-sz300 |
| `project-brand-context.md` | 品牌"刘姐菜篮子"、默认店铺"太平店" |

---

## 六、评估结论

**sz300 是一个完整的菜市场智能秤 SaaS 后端系统，不是框架的演示示例。**

- 7,068 行业务代码
- 10 个 Controller + 13 个 Model + 9 个 Service
- MQTT 硬件通信（与智能秤实时交互）
- AI 商品分类
- OTA 固件升级
- 完整的认证/商户/商品/设备/订单/文件管理
- CORS + CSRF + JWT 安全中间件链

**当前状态**：代码存在于 sz-rust-oss 和服务器企业版仓库，不在当前 sz-rust 开源 workspace 中。README 引用 sz300 作为生产接线证据时，应标注"企业版功能"。

---

*生成：2026-09-19，基于代码实际深读，非文档自述*