# 评估报告准确性核验（2026-09-20）

> **触发**：对比《2026-09-19-评估报告对比分析.md》与《2026-09-19-sz300究竟是什么-基于代码实际评估.md》两份报告，核对哪份准确、结论是否基于实际代码。
> **方法**：逐项以源码 + git + 外部仓库实测交叉验证，不作文档自述背书（铁律 23 证据链）。
> **审查时点**：当前 HEAD `d416e93`（`git describe` = `v1.2.2-87-gd416e93`）。
> **修订（2026-09-20 二次复核）**：本报告初版有 1 处实测值不实 + 多处口径不精确，同日二次复核修正如下（各项均附验证命令）：
> ① 测试函数实测值 7,134 → **7,122**（与原报告零差异，原"差12行级噪声"系误测）；② `.rs`/代码行数标注 packages 口径（全仓 597 文件 / 231,593 行，差额 193 行 = scripts/perf-compare 5 个 bench main.rs，两时点 bench 未变已证）；③ oss members 27 → **26**；④ 07a080d 距今 ~10 → **8** commit；⑤ CHANGELOG 最新 Unreleased 段为 09-14，**无 09-20 条目**；⑥ 补注 `d81964d` 在全部四个相关仓库均不存在；⑦ 补强 cc8b21a/d403420/5ef4fb1 在 E:/www 企业版仓库实查为 commit；⑧ README 17 处为 HEAD 口径（工作树未提交改动后 18）。

---

## 一、总体判定

| 报告 | 判定 | 摘要 |
|------|------|------|
| 《2026-09-19-评估报告对比分析.md》 | ✅ **大体准确**（方向正确，数字基本吻合） | 正确识别 sz300 移出为本源；crate 数/文件数/commit 存在性与报告一致 |
| 《2026-09-19-sz300究竟是什么-基于代码实际评估.md》 | ✅ **高度准确**（抽验 100% 吻合） | 7068 行、10 控制器/13 模型/9 服务、路由清单全部实测相符 |

**核心结论**：两份报告数字来自真实代码，无幻影交付。二者的"矛盾"是**仓库分离事件造成的口径错位**（09-04 看完整仓库含 sz300，本次看开源版接线），并非说谎。

---

## 二、仓库拓扑（本次核实的关键修正）

可能存在多个仓库，兹将实测路径逐一列明（这是后续任何评估的前提）：

| 仓库路径 | crate 组成 | 是否含 sz300 | 说明 |
|---|---|---|---|
| `E:/vue/test/鲜视达/rust/sz-rust`（当前工作区） | 32 workspace members | ❌ 不含 | 开源版框架；报告审查点基准 |
| `E:/vue/test/鲜视达/rust/sz-rust-oss` | 26 members | ✅ 含 `sz-rust-sz300`（7,068 行） | 含 sz300 的历史/源码仓库 |
| `E:/vue/test/鲜视达/rust/sz-rust-enterprise` | **7 members**：addons-operate/crm/erp/ecommerce/cms/forum/im | ❌ **不含** | 业务插件企业版（用户指认路径），**无 sz300** |
| `E:/www/rust/sz-rust-enterprise` | 含 sz300 + sdd + migration + 7 addons | ✅ 含 `sz-rust-sz300` | 企业版（含 AI 编排/迁移工具） |

> **⚠️ 关键修正**：用户本次指认的"企业版"是 `E:\vue\test\鲜视达\rust\sz-rust-enterprise`，该目录**不含 sz300**，只有 7 个业务插件。sz300 所在的"企业版"实为 `E:/www/rust/sz-rust-enterprise`。评估引用 sz300 的企业版路径时，应以 `E:/www/...` 为准。
> 此外，`sz-rust-oss` 也被称为"oss"，但其成员中含 sz300，命名与实际内容需注意。

---

## 三、《评估报告对比分析》逐项核验

| 报告声称值 | 实测值 | 判定 |
|---|---|---|
| workspace members = 32 | `Cargo.toml [workspace]` 列出 32 个 | ✅ 一致 |
| 审查点 `07a080d`（早于当前 ~10 commit） | `git rev-list --count 07a080d..HEAD` = 8 | ✅ 存在（实距 8 个 commit，"~10"系估算偏大，已修正） |
| `.rs` 文件 = 592（07a080d 时点） | packages 口径实测 592；全仓 `git ls-tree -r` 为 597（多出 scripts/perf-compare 5 个 bench main.rs） | ✅ 完全一致（packages 口径，与原报告 `find packages` 方法同口径） |
| 代码行 = 230,580（07a080d） | packages 口径：07a080d = 230,580、当前工作树 = 231,400（全仓口径 230,773 / 231,593，差额恒为 193 行 = 5 个 bench 文件；`git diff --stat 07a080d HEAD -- scripts/perf-compare` 为空，证 bench 两时点未变） | ✅ 精确吻合（packages 口径） |
| 测试函数 = 7,122 | 复现报告正则（`#[test]\|#[tokio::test]`）于 07a080d 实测 **7,122**（`git grep -hE` 行数口径与 `-o` 出现次数口径均为 7,122；HEAD = 7,173） | ✅ 完全一致（零差异；初版记 7,134/差12 系复核误测，已修正） |
| `d81964d`（09-04 评估基准）不存在 | `git cat-file -t` → NOT-FOUND（本仓库、sz-rust-oss、鲜视达/rust/sz-rust-enterprise、E:/www/rust/sz-rust-enterprise 四仓库均 NOT-FOUND） | ✅ 不存在（四仓库皆无——09-04 报告若以其为基准 commit，该基准为幻影） |
| `cc8b21a`/`d403420`/`5ef4fb1` 不存在 | 三个均 NOT-FOUND（本仓库）；`git -C E:/www/rust/sz-rust-enterprise cat-file -t` 三者均返回 commit | ✅ 企业版 commit，不在开源仓库（已实查 E:/www 企业版仓库证实） |
| `1614e84`（分离 commit）存在 | `git cat-file -t` → commit | ✅ 存在 |
| README 引用 sz300 17 处 | `git show HEAD:README.md \| grep -c sz300` = 17（HEAD 口径；工作树含未提交改动为 18） | ✅ 完全一致（HEAD 口径） |
| 覆盖率 70.8%（本次会话口径） | 09-14 llvm-cov 开源 workspace 行覆盖 **91.44%**（CHANGELOG） | ⚠️ 口径偏窄：70.8% 剔除了 sz300 集成，91.44% 才是当前开源版全量 |

**结论**：该报告方向正确、关键 commit 存在性判定全部正确。唯一需补注：其审查点 `07a080d` 距今又推进 8 个 commit（facade 幻影测试消除、覆盖率提升、CLI 接线），属"时点快照"。

---

## 四、《sz300 究竟是什么》逐项核验

核验对象路径：`E:/vue/test/鲜视达/rust/sz-rust-oss/packages/sz-rust-sz300`

| 报告声称值 | 实测值 | 判定 |
|---|---|---|
| 7,068 行 Rust | `find src -exec cat {} + \| wc -l` = **7,068** | ✅ 精确吻合 |
| 10 个 Controller | `ls src/controllers` 去 `mod.rs` = 10 | ✅ |
| 13 个 Model | 去 `mod.rs` = 13 | ✅ |
| 9 个 Service | 去 `mod.rs` = 9（含 mqtt_service + mqtt_listener） | ✅ |
| 路由清单 | `router.rs` 逐条 route 完全对应 | ✅ |
| 中间件链 CORS→CSRF→JWT | 公开路径白名单逻辑吻合 | ✅ |
| sz300 不在当前 workspace | 当前 packages/ 无 sz300 | ✅ |
| sz300 在 oss + 企业版 | oss 存在；企业版在 `E:/www/...`（详见 §二） | ✅（路径见修正） |

**结论**：全维度 100% 吻合，是该话题最可信的一份实证文档。唯一修正点是企业版路径表述（q.v. §二）。

---

## 五、遗留问题（比对后暴露，供后续跟进）

1. **README 版本号陈旧**：`README.md:7` 仍写 `v1.2.0`，实际 `git describe` = `v1.2.2-87-gd416e93`，CHANGELOG 最新 Unreleased 段为 2026-09-14（无 09-20 条目，09-20 两个提交系改写 09-14 段内容）。"版本号修正"只做了一部分。（**复核更新**：工作区已见未提交修正——README:7 改为 v1.2.2 并注明 tag/HEAD 偏移，随该改动提交后本项关闭）
2. **README 17 处 sz300 引用**：均把 sz300 作为"生产已接入"证据，但 sz300 代码在 `sz-rust-oss` 与 `E:/www/rust/sz-rust-enterprise`，不在开源架构内。建议在 README 标注"企业版功能"，避免开源读者误以为开源版自带。事实层面：`docs/implementation-progress.md` 组11 记录 `122.51.216.76:8300` 服务器真实部署（health 200），支持"sz300 生产存在"这一结论。（**复核更新**：工作区已见未提交修正——README 新增"企业版口径说明"段落标注 sz300 不在开源 workspace 内，随该改动提交后本项关闭）
3. **仓库命名易混淆**：`sz-rust-oss` 与当前 `sz-rust`、两个 `sz-rust-enterprise` 并存，且 oss 含 sz300、一个 enterprise 含 sz300、另一个 enterprise 不含——后续评估务必先确认目标仓库路径。

---

## 六、成熟度综合判定（基于实测）

- **工程成熟度**：当前开源版 32 crate、592+ .rs、约 23 万行、7000+ 测试、门禁体系完整（变异测试/AI 评审/覆盖率基线/ADR 审计链）。09-14 llvm-cov 开源 workspace 行覆盖 **91.44%**。可判 **B+ 至 A-**（口径：是否计入外部 sz300+企业版）。
- **产品成熟度**：C。框架能力全，但唯一真实生产应用（sz300，7,068 行 + MQTT/OTA/AI 分类/完整路由）已移出开源仓库，开源版缺开箱即用的业务闭环 demo。
- **版本节奏**：已打至 v1.2.2，文档同步略滞后。

**最终结论**：09-04 报告的 **B+** 在"能力完整性"维度成立；本次评估 B- 77.65 在"开源版接线"维度成立，但覆盖率数字偏窄。两份报告互补后才是全貌，均无幻影交付。

---

*核验基于：当前 HEAD `d416e93`、`git ls-tree/cat-file`、`find/wc/grep` 实测、外部仓库 `sz-rust-oss` 与两个 `sz-rust-enterprise` 实查。生成 2026-09-20；同日二次复核以 `git rev-list/cat-file/grep/diff` 复现并修正文首所列各处。*