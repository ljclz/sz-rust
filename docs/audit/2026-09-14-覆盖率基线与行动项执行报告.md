# OSS Workspace 覆盖率基线报告（2026-09-14）

> **时点锚定**：`main @ 14b0b25`（含 Oracle WIP 工作区：Cargo.toml sz-orm 6.2.0→6.9.0 + sz-rust-cli Oracle 接入未提交改动）
> **命令**：`cargo llvm-cov --workspace --exclude sz-orm-macros --exclude sz-rust-macros --summary-only -j 2`（cargo-llvm-cov 0.9.0，exit 0）
> **口径**：全部常规测试（DB 集成 --ignored 测试未运行，与 CI coverage job 的 regular-tests 步骤同口径）；原始日志全量存证：`docs/audit/2026-09-14-coverage-baseline-full.txt`（505 个 .rs 文件）
> **与 08-15 旧值（8.7%）不可比**：旧值为 sz300 lib 窄视角 tarpaulin（lib 测试不含集成测试）；本基线为全 workspace 全测试目标 llvm-cov。

## 一、总量

| 指标 | 数值 |
|------|------|
| **行覆盖** | **91.44%**（14722/171948 missed） |
| 区块覆盖 | 87.15% |
| 函数覆盖 | 89.84% |

**结论：workspace 行覆盖 91.44%，已高于 85% 门槛。** 08-15 的「8.7% 最实质测试缺口」结论系窄口径所致，正式更正。

## 二、分 crate 明细（升序，<85% 为重点）

| crate | 行覆盖 | 未覆盖/总行 | 说明 |
|-------|--------|------------|------|
| sz-rust-examples | 2.05% | 1389/1418 | 示例/演示代码，无断言价值，**已列入豁免清单**（coverage-exemption.md，2026-09-14） |
| sz-rust-addons-admin | 39.89% → **44.75%**（2026-09-14 补测后） | 2266/5064 | **商业化核心插件的真实缺口**：21 端点集成测试仅覆盖路由层。已补 `tests/services_test.rs` 17 测试（user_service.rs 68.59%，create/delete/update 验证与事务分支）；role_service 18.09% 为下一优先 |
| sz-rust-marketplace | 60.16% | 874/2194 | 市场插件，CLI plugin 命令真实消费 → **裁定保留**（2026-09-14，非孤儿） |
| sz-rust-visual | 72.04% | 215/769 | **裁定保留**（2026-09-14）：独立 Tauri 产品线（ADR-034），自建 visual-build.yml CI workflow + main.rs 入口，非孤儿 |
| sz-rust-workflow | 84.84% | 873/5757 | 临界，capability 模块 66% 为主要拖累 |
| sz-rust-cli | 87.83% | 1316/10811 | 其余 22 crate 均 ≥89% |

其余 22 crate：89.05%（tracing）～ 98.49%（orm-ext-facade），全部 ≥85%。

## 三、pay.rs 与变异测试退出条件（DB-2026-09-03-01）

- `packages/sz-rust-core/src/pay.rs` 仅为 re-export 垫片（`pub use sz_rust_pay_facade::pay`，core lib.rs:141）
- **真实支付逻辑 `sz-rust-pay-facade/src/pay.rs` = 95.98%**（1268 行 / 51 missed）≥ 75% 退出条件
- **已执行**：`**/pay.rs` 从 `.cargo/mutants.toml` exclude_globs 移除（2026-09-14）；`cargo mutants --list` 确认 71 个 pay 变异体入列
- 全量重跑：`cargo mutants -p sz-rust-pay-facade --timeout 120 -j 2`（预计 ~1-2h）

## 四、后续建议

1. **per-crate 门禁收口路径**：coverage job 当前 `continue-on-error: true`（addons-admin 等会被 85% per-crate 门槛拦住）。两条路：① 对 examples（演示代码）走豁免清单，addons-admin/marketplace/visual 补测或豁免后移除 continue-on-error
2. **addons-admin 补测**为商业化优先级最高项：services 层（user_service/role_service/menu_service/config_service）单测 + 既有 21 端点集成测试互补
3. **S1 阶段机制**：workspace 总量已达 91.44%，`[coverage-stage:S1]`（30%）已无意义，直接按 85% 目标管理 per-crate 缺口
4. **marketplace/visual 存废裁定**：与文档定性（关联 KNOWN_FICTIONAL）矛盾，建议参照 k8s-operator 模式处理（移除或补测转正）

## 五、复现

```bash
cargo llvm-cov --workspace --exclude sz-orm-macros --exclude sz-rust-macros --summary-only -j 2
# 分 crate 聚合（Git Bash）：
# tr '\\' '/' < 日志 | awk -F'  +' '/^sz-rust/ && $1 ~ /\.rs$/ {split($1,a,"/"); t[a[1]]+=$2; m[a[1]]+=$3} END {...}'
```
