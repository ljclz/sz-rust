# sz-rust 项目状态与后续开发参照文档（2026-09-13）

> **时点锚定**：`main @ 52bb534`（已 push origin，`2c8f399..52bb534`）。本报告为时点快照，后续提交不在范围内。
> **2026-09-14 更新**：行动项 4（k8s-operator 按 ADR-038 补执行删除）已完成；行动项 6 三条框架缺口核实**全部已闭合**（patch/head/options、group/nest、模板 include 均已实现，08-21 审计该三项结论过时），无需写码。详见 `docs/audit/2026-09-14-覆盖率基线与行动项执行报告.md`。
> 前作：`docs/audit/2026-08-19-项目状态与后续开发参照.md`；综合评估基线：`docs/audit/2026-08-21-综合深度审计报告.md`（89.4/100）。
> 本报告所有数字均附来源（命令输出 / 报告路径 / commit SHA），未实测项显式标注「待重测」。

## 一、项目概况

- **定位**：对标 ThinkPHP 8 的 Rust Web 框架（开源版，Apache-2.0）+ 企业版仓库物理分离（`E:\vue\test\鲜视达\rust\sz-rust-enterprise`，7 个行业插件 addons-{cms,crm,ecommerce,erp,forum,im,operate} 实存已核验）
- **仓库结构**：workspace 29 crate（`Cargo.toml` members）；sz300 业务包已于 `1614e84`（09-05）移出，测试/发布职责移交企业版流程
- **并行开发中**：工作区存在未提交 Oracle 接入 WIP（`Cargo.toml` sz-orm 6.2.0→6.9.0、`sz-rust-cli` 的 admin/migrate/cli 及 CHANGELOG.md，另有 12 个 test_oracle_*.js 未跟踪脚本）——合入时必须走完整审查流程

## 二、本阶段完成总览（2026-09-05 ~ 09-13，按提交序）

| 提交 | 内容 |
|------|------|
| `f6b0e7e` | Admin 插件 CLI 接线（migrate/list-routes/list-capabilities/init） |
| `d1b0061` | 审查门禁代码修复：admin.rs:224 裸 unwrap→`ok_or(CliError::Clap)`；`user_service.rs` password 补 `skip_serializing`；multi_tenant_demo 3 处 unwrap 改错误传播 |
| `ae27e57` | pr-review.sh sz300 存在性守卫（修复 1614e84 后 G11/G12 结构性失败） |
| `d14f9eb` | 请求 DTO 手工 Debug 脱敏（password→`[REDACTED]`，AI 评审采纳项） |
| `a6928b9` | 幻影声称批量核验闭环：doc-code 脚本新增 `ENTERPRISE_DELIVERED_CRATES` 类别（129 ERROR→0）；coverage.yml/release.yml 幻影包实修（**release/coverage CI 自 09-05 起实际是坏的，本次才修复**）；ADR-036 校正；README 双语企业版标注 |
| `8618a77` | 4 个无断言测试转行为断言（PATCH 405 路由探测法 / guard 401 探测 / broadcast try_recv 无回放） |
| `d6a1f8c` | AI 评审风格建议采纳：删除 if:false 禁用 job；release 新增 `targets` job 单一来源（矩阵+断言名单同源）；README 🔒 标注 |
| `9e75467` | ci.yml 迁移残留清零：删除 if:false 的 db-integration job + coverage job 的 sz300 死步骤（该步骤每跑必败靠 continue-on-error 静默放行） |

**终态**：`docs/audit/2026-09-13-pr-review-main.md` @ `9e75467` —— **0 critical / 0 high / 0 medium / 2 low**（2 条 gate-skipped 为 sz300 守卫预期结构记录，非缺陷）✅ 通过。origin/main = `52bb534`。

## 三、质量门禁体系现状（当前最强项）

- **15 项门禁 + 严重度模型 + AI 评审 + 事件留痕**（`scripts/audit/pr-review.sh`；事件 `docs/audit/events.jsonl`）
- 门禁实测数字（2026-09-13）：
  - 铁律 2 裸 unwrap：**0**（`check-unwrap.py`）
  - 敏感字段 EXPOSED：**0**（`SAFE_CUSTOM_DEBUG 7`）
  - doc-code 幻影声称 ERROR：**0**（WARN 145 处不阻塞，见第六节）
  - assertion-value ERROR：**0**（全仓 6148 个测试函数 / 13357 处断言，密度 2.17）
  - ADR 漂移 ERROR：**0**
- **AI 评审**：CSDN coding 端点 + `deepseek-v4-flash`（export `AI_API_KEY`/`AI_BASE_URL=https://ai.csdn.net/api/model/coding/v1`/`AI_MODEL=deepseek-v4-flash`，`--ai-timeout 300`）；四轮实测可采纳率 2/5→0/5 递减，意见须逐条复核（曾出现「复述 diff 已含修复」型幻觉）
- 本阶段体系自证：审查体系发现并当日闭环了仓库分离造成的结构性漂移（门禁脚本、release/coverage CI、文档、测试四类），fail-closed 有效

## 四、成熟度评估（分维度）

| 维度 | 成熟度 | 证据 / 缺口 |
|------|--------|------------|
| 框架功能 | ★★★★ | 路由/中间件/DI/模板/缓存/插件/多租户/数据权限/Admin/AI 全家桶已落地并有生产接线（08-21 报告 §4.1 确认 AI 栈非幻影） |
| 质量门禁 | ★★★★★ | 见第三节（最强项） |
| 安全 | ★★★★ | 白帽 9/9 + 黑帽 16 项复核修复（历史提交 1152ba4/91f5c38/737c31e/1ac9c77/318b29b）+ 今日 password 双层脱敏 |
| 商业化分离 | ★★★★ | OSS/enterprise 分离且一致性已收口（CI 零幻影引用、README 🔒、发布产物 = OSS CLI） |
| 测试覆盖 | ★★ | **行覆盖率 8.7% 实测 vs 85% 门槛**（08-15 tarpaulin，sz300 视角，**已过时——sz300 已移出，OSS workspace 待重测**）；CI 已建分阶段爬坡（commit message `[coverage-stage:S1..S4]` → 30/50/70/85）未启用 |
| 生态/文档 | ★★★ | crates.io 已发版；145 处 WARN 待核验；README 版本号/测试数不一致欠账（08-21 报告 §4.1） |

## 五、生产就绪阻塞项

1. **行覆盖率**（最实质缺口）：先用 `cargo llvm-cov --workspace`（或 tarpaulin）对 OSS workspace 重测基线，再按 S1（30%）→S4（85%）逐级爬坡
2. ~~**k8s-operator 孤儿 crate 处置**~~（✅ 2026-09-14 完成：按 ADR-038 补执行目录物理删除，债务 DB-2026-08-20-05 随之 RESOLVED）

## 六、债务清单（截至 2026-09-13，`docs/audit/doc-debt.md`）

| ID | 内容 | 限期 | 状态 |
|----|------|------|------|
| DB-2026-08-20-05 | k8s-operator reconcile 错误传播测试缺口 | 09-03 | RESOLVED（2026-09-14：按 ADR-038 物理删除孤儿 crate，缺口随移除消灭） |
| DB-2026-09-03-01 | pay.rs 行覆盖 ≥75% 后必须移出 mutants.toml 排除清单并重跑 `cargo mutants -p sz-rust-core` | 09-30 | PENDING |
| DB-2026-09-13-03 | 本阶段 6 笔修复的 CHANGELOG 补记（工作区 CHANGELOG 有 Oracle WIP，随其合入一并补） | 09-27 | PENDING |

另：doc-code 145 处 WARN 需核验（企业版归属/历史回退标注确认/误导引用删除），不阻塞但有核验义务。

## 七、后续开发行动项（对照清单，按优先级）

1. **覆盖率基线重测**（先做，为一切覆盖率结论换真值）
   验收：产出基线报告至 `docs/audit/`，注明测量视角与命令
   命令：`cargo llvm-cov --workspace --summary-only -j 2`（单 crate 可避免 OOM：`cargo llvm-cov -p <crate> --summary-only`）
2. **Oracle WIP 合入**（用户并行开发中）：走完整审查 `bash scripts/audit/pr-review.sh --range HEAD~1..HEAD --ai --ai-timeout 300`；同一提交内补记 CHANGELOG（连带清偿 DB-2026-09-13-03）
3. **pay.rs 变异退出条件**（09-30 前）：pay.rs 行覆盖 ≥75% → 从 `.cargo/mutants.toml` exclude_globs 移除 → `cargo mutants -p sz-rust-core --timeout 120 -j 2`
4. ~~**k8s-operator 处置裁定**~~（✅ 2026-09-14 完成：ADR-038 于 08-21 已决策移除，本次补执行目录物理删除；doc-code/adr 两门禁脚本 REMOVED 清单同步，README 双语校正——en 版原遗留「Implemented」矛盾表述）
5. **145 处 WARN 批量核验**：企业版归属确认 / 历史回退标注确认 / 误导引用删除（重点：README 核心 README.md:39-45 的 sz300 生产接线声称，业务包已移交企业版）
6. ~~**框架层小缺口**~~（✅ 2026-09-14 核实：08-21 审计 §4.2 三条——RouterBuilder patch/head/options（router.rs:359-381）、路由分组 group()/nest()（router.rs:455/478）、模板 `{include}`（mvc-facade view.rs:291-297）**均已实现**，审计结论过时，无需写码）

## 八、关键技术决策记录（本阶段新增）

1. **sz300 门禁守卫模式**：`pr-review.sh` 以 `SZ300_IN_WS`（grep Cargo.toml）探测，包不在 workspace 时门禁降级 + 记 `gate-skipped`（low 不阻塞）——开源版审查不因企业版职责阻塞
2. **文档声称四分类**：doc-code 脚本对不存在 crate 按 KNOWN_FICTIONAL（ERROR）/ REMOVED（WARN）/ **ENTERPRISE_DELIVERED**（WARN，2026-09-13 新增，企业版仓库实存核验）/ 未知（ERROR fail-closed）分级
3. **路由挂载测试的 PATCH 探测法**：已注册路径 × 未注册方法 → 405，未注册路径 → 404；免 mock 池业务语义干扰，可批量验证端点清单
4. **禁用 job 处理模式**：迁移 job 直接删除 + 原地注释 + git 历史留痕（不用 if:false / workflow_dispatch——后者对 tag 触发器无效）
5. **发布平台名单单一来源**：release.yml `targets` job outputs 派生 build matrix 与产物断言 EXPECTED

## 九、验证命令速查（2026-09-13 实测可用）

```bash
# 全量审查（含 AI 评审；AI 环境变量三件套，缺 key 时 missing-key=medium 阻塞）
export AI_API_KEY=... AI_BASE_URL=https://ai.csdn.net/api/model/coding/v1 AI_MODEL=deepseek-v4-flash
bash scripts/audit/pr-review.sh --range HEAD~1..HEAD --ai --ai-timeout 300

# 快查三门禁
node scripts/audit/doc-code-consistency.js
node scripts/audit/assertion-value-check.js
node scripts/audit/adr-code-consistency.js

# 编译/静态（Windows：-j 2 防页面文件不足；CARGO_TARGET_DIR 已指 F 盘禁写 C）
cargo check --workspace --all-targets -j 2
cargo clippy --workspace --all-targets -j 2 -- -D warnings
cargo fmt --all --check

# 单 crate 测试（避免 OOM，禁止 --workspace）
cargo test -p sz-rust-orm-facade --lib -j 2

# push（main 无 upstream 跟踪，需显式指定；走本地代理）
https_proxy=http://127.0.0.1:1080 http_proxy=http://127.0.0.1:1080 git push origin main
```

## 十、下一步规划（按周）

- **本周**：覆盖率基线重测（行动项 1）→ 启用 `[coverage-stage:S1]`
- **两周内**：Oracle WIP 合入 + CHANGELOG（行动项 2）；k8s-operator 裁定（行动项 4）
- **09-30 前**：pay.rs 变异退出（行动项 3）
- **滚动**：145 处 WARN 核验（行动项 5）；框架小缺口（行动项 6）
