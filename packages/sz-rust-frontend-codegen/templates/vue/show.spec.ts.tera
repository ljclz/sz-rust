import { mount } from '@vue/test-utils'
import Show from '../Show.vue'

describe('{{ module_name }} Show', () => {
  it('renders', () => {
    const wrapper = mount(Show)
    expect(wrapper.exists()).toBe(true)
  })
})