/target
*.log
*.tmp

# 敏感信息预防性排除
.env
.env.*
*.key
*.pem
*.p12
*.pfx
secrets.json
credentials.json
deploy_key
deploy_key.pub
# SSH 私钥（服务器验证用，曾以无扩展名形式存于仓库根，禁止提交）
ssh_key
ssh_key.pub
# Oracle 服务器验证脚本（硬编码服务器 IP/凭据，仅本地使用）
install_oracle_client.js
test_oracle_*.js
tmp_*.js
# Windows 保留名误创建文件
nul

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# 临时编译错误日志
errors*.txt
errors*.log

# 测试覆盖率输出（开发临时文件）
coverage/


# cargo-mutants 输出（变异测试产物）
mutants.out/
mutants.out.old/
# CodeGraph 索引（本地工具产物）
.codegraph/
# 临时测试脚本
_*.py

# ZCode agent
.zcode/
# Coverage raw data（llvm-cov 运行时产物，含 visual 包实测产生的 default_*.profraw）
*.profraw
# Python 缓存（scripts/perf 工具）
__pycache__/
# Inscode IDE 本地目录
.inscode/

# WASM build artifacts
packages/sz-rust-wasm/target/

# Benchmark comparison build artifacts (4 frameworks × ~300MB)
scripts/perf-compare/benchmarks/*/target/

# 部署脚本（含 SSH 密钥，禁止提交）
scripts/deploy/

# Artifacts
artifacts/
*.tar.gz
*.bundle

# CodeArts Doer 本地产物（规划文档、中间产物）
.codeartsdoer/
